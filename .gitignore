.github/memory/
.github/research/
.vscode/

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test output
out/
test_output/
.env
.pytest_cache/
.ruff_cache/

# VS Code extension build copies — source of truth is src/interact/data/
vscode-extension/src/models.json
vscode-extension/src/benchmarks.json
vscode-extension/src/settings.json

# personal scratch prompt — never commit
.github/prompt.txt

# Per-developer prompt overlay — each developer materializes their OWN customizations
# (agents / instructions / prompts / skills) here via `python scripts/sync_prompts.py`.
# Never committed: the repo carries no one's set. See CLAUDE.md "Per-developer prompts".
.github/agents/
.github/instructions/
.github/prompts/
.github/skills/
